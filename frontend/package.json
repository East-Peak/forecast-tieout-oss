{
  "name": "forecast-tieout-frontend",
  "version": "0.1.0",
  "description": "Forecast Tieout: Reconcile the revenue plan against pipeline reality.",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:data": "node ./scripts/sync-data.mjs",
    "scenario:api": "echo 'Scenario API not configured — set VITE_SCENARIO_API_URL to use a backend service'",
    "dev": "npm run sync:data && vite",
    "build": "npm run sync:data && tsc && vite build",
    "preview": "npm run sync:data && vite preview",
    "test": "vitest",
    "test:e2e": "npm run build && playwright test --grep @smoke",
    "test:visual": "npm run build && VISUAL=1 playwright test --grep @visual",
    "test:visual:update": "npm run build && VISUAL=1 playwright test --grep @visual --update-snapshots",
    "knip": "knip",
    "verify": "knip && vitest run"
  },
  "dependencies": {
    "@fontsource/space-grotesk": "^5.2.10",
    "@supabase/supabase-js": "^2.57.4",
    "@vercel/analytics": "^2.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "recharts": "^2.15.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^25.5.0",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.2",
    "autoprefixer": "^10.4.20",
    "jsdom": "^29.1.1",
    "knip": "^6.24.0",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
