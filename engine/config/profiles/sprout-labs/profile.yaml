org_name: "Sprout Labs"
org_id: "sprout-labs"
timezone: "US/Pacific"

# Feb-start fiscal year (Feb 1 - Jan 31), aligned with the public demo set.
fiscal_year_start_month: 2

revenue_metric: "bookings"

# Declarative backend selection.
data_access:
  type: csv
  params:
    path: engine/data/sprout-labs

# Story: early-PMF startup pushing for $10M ARR. H1 coverage looks plausible,
# but H2 assumes repeatable funnel motion before MQL volume or pipeline depth
# proves it.
description: "Sprout Labs — $10M early-PMF plan, repeatability not proven"
